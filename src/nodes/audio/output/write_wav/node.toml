name = "audio.output.write_wav"
version = "1.0.0"
display_name = "Write WAV"
description = "Records input audio as a WAV file"
order = 32
gui = ["reactflow", "headless"]
run_when_stopped = true

[[ports]]
name = "audio"
data_type = "audio"
direction = "in"
